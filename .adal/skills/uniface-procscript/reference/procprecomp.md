---
title: "ProcScript Preprocessor Directives"
category: "Uniface 9.7 ProcScript Reference"
entries: 23
---

# #comment

Define a comment in the generated code.

#comment`;`Comment

For example:

```procscript
#comment ; Start MUSICSHOP:HandleError
...
#comment ; End MUSICSHOP:HandleError
```

---

# #define

Define a constant name and value.

#define  Constant {`=`} Expression

## Parameters

* Constant—name of the
  constant
* Expression—expression that
  evaluates to a constant value

## Description

The #define directive defines
a constant name and value.

You can use constants as operands in expressions
in Proc code by placing the name of the constant between angle brackets
(`<>`). When a component is compiled, the Proc compiler replaces each
reference to a constant by the expression that has been defined for that constant.

The following example defines a constant MYCONSTANT with the text `I'm
defined`. If the constant exists, the Proc compiler generates a warning with the text of
MYCONSTANT:

```procscript
#define MYCONSTANT = I'm defined
#ifdefined MYCONSTANT
   #warning <MYCONSTANT>
#endif
```

This results, after compilation, in the following
message in the message frame:

```procscript
warning: 1000 - I'm defined
```

## Related Topics

- [#undefine](_undefine.md)
- [#ifdefined](_ifdefined.md)
- [#ifundefined](_ifundefined.md)


---

# #entry

Insert a global Proc entry.

#entry  Library:GlobalProc

## Parameters

* Library—name of the library
  containing the global Proc entry
* GlobalProc—name of the
  global Proc entry

## Description

The #entry directive enables
you to include global Proc into Proc code so that it is treated as local Proc. The
#entry is resolved at compile time.

History

| Version | Change |
| --- | --- |
| 9.4.01 | Introduced |

## Related Topics

- [entry](../../procstatements/entry.md)


---

# #error

Produce a compiler error message.

#error  Text

## Parameters

Text—message text

## Description

The directive
#error causes the Proc compiler to generate an error
message. The message
Text appears in the message frame, and the error is added
to the total for the module being compiled.

The following example causes
the Proc compiler to generate an error message if the compile-time constant
`<MYCONSTANT>` has not been defined:

```procscript
#ifundefined MYCONSTANT
   #error The constant MYCONSTANT has not been defined.
#endif
```

## Related Topics

- [#info](_info.md)
- [#warning](_warning.md)


---

# #for

Generate blocks of repeated Proc code.

#for  Item`=``(`List`)` {`,`Counter}

... Proc statements and precompiler
directives ...

#endfor

## Parameters

* Item—constant that contains
  an item of List.
* List—list of items
  separated by commas, spaces, or GOLD semi-colon (;)
* Counter—constant containing
  a sequential number, starting with 0, identifying the position of item of Item
  in the List

Both Item and
Counter are defined only during processing of the #for block.

## Description

The #for directive generates a
repeating block of Proc code for each item in a list.

In the following example, the compile-time
constant `<MYLIST>` has been defined as `A,B,C`:

```procscript
#for ITEM = (<MYLIST>), COUNTER
   message "Element <COUNTER> in the list is <ITEM>"
#endfor
```

The Proc code generated by this
#for block is:

```procscript
message "Element 0 in the list is A"
message "Element 1 in the list is B"
message "Element 2 in the list is C"
```

---

# #if

Generate Proc code conditionally.

#if`(`Expression 1`)`

... Proc statements and precompiler directives

{#elseif`(`Expression
2`)`

... Proc statements and precompiler directives ...

... }

{#else

... Proc statements and precompiler directives ... }

#endif

## Parameters

Expression—logical expression that evaluates to true or false

## Description

The #if directive generates Proc code conditionally based on the
results of logical expressions. Each Expression is evaluated in sequence,
beginning with the one with the #if statement.

The Expression can contain one or more of the relational operators,
`=` (equal to) or `!=` (not equal to), in combination with the
logical operators `&` (and), and `|` (or). Parenthesis can be
used to create nested expressions.

The following example adds a
debug statement to the Proc code being compiled if the
constant
`<DEBUGGING>` is defined as 1:

```procscript
#ifdefined DEBUGGING
   #if (<DEBUGGING>=1)
      debug
   #endif
#endif
```

* If the constant
  `<DEBUGGING>` is not defined, no further code is generated.
* If the constant
  `<DEBUGGING>` is defined as 0, the
  #if  directive is generated as follows:

  ```procscript
  #if (1=0)
     debug
  #endif
  ```

  In this case, no Proc code is generated.
* If the constant
  `<DEBUGGING>` is defined as 1, the
  #if  directive is generated as follows:

  ```procscript
  #if (1=1)
     debug
  #endif
  ```

  In this case, the following Proc statement is compiled:

  ```procscript
  debug
  ```

---

# #ifdefined

Test if a constant has been defined.

#ifdefined  Constant

...
Proc statments and precompiler directives ...

#endif

## Parameters

Constant—constant name

## Description

The
#ifdefined directive can be used to create a block of Proc
statements and precompiler directives whenever the constant specified already
exists.

The following example adds a
debug statement to the Proc code being compiled if the
constant
`<DEBUGGING>` is defined as 1:

```procscript
#ifdefined DEBUGGING
   #if (<DEBUGGING>=1)
      debug
   #endif
#endif
```

* If the constant
  `<DEBUGGING>` is not defined, no further code is generated.
* If the constant
  `<DEBUGGING>` is defined as 0, the
  #if  directive is generated as follows:

  ```procscript
  #if (1=0)
     debug
  #endif
  ```

  In this case, no Proc code is generated.
* If the constant
  `<DEBUGGING>` is defined as 1, the
  #if  directive is generated as follows:

  ```procscript
  #if (1=1)
     debug
  #endif
  ```

  In this case, the following Proc statement is compiled:

  ```procscript
  debug
  ```

## Related Topics

- [#ifundefined](_ifundefined.md)


---

# #ifundefined

Test if a constant has not been defined.

#ifundefined  Constant

...
Proc statments and precompiler directives ...

#endif

## Parameters

Constant—contant name

## Description

The
#ifundefined directive can be used to create a block of
Proc statements and precompiler directives whenever the constant specified does
not already exist.

In the following code, if the
constant
`<DEBUG_ALLOWED>` has not been defined, a compiler information
message is generated and a
nodebug Proc statement is generated and compiled.

```procscript
#ifundefined DEBUG_ALLOWED
   #info Debugging not enabled.
   nodebug
#endif
```

## Related Topics

- [#ifdefined](_ifdefined.md)


---

# #include

Copy the specified include Proc.

#include {Library:}Module

## Parameters

* Library—name of the library
  containing Module. If omitted, the library of the current component, global
  Proc, menu, and so on, is used, as appropriate.
* Module—name of an include
  Proc

## Description

The #include directive copies
the contents of the specified include Proc into the Proc code being generated.

For example, the component MYCOMP contains a
component variable, $TRIG\_TRACE$, that can be set dynamically at run time to turn trigger ‘tracing’
on and off. Whenever this variable is TRUE, a message is added to the message frame at the start of
each trigger.

Each trigger in MYCOMP begins with an
#include directive referencing the include Proc START\_TRIG in library MYLIB; for
example:

```procscript
#include MYLIB:START_TRIG
```

The include Proc START\_TRIG in library MYLIB
contains the following:

```procscript
;Trigger: <$triggerAbbr>
if ($TRIG_TRACE$)
   putmess "Entering trigger <$triggerAbbr>"
endif
```

For a field-level Detail trigger, the precompiler
hands the following Proc code to the compiler:

```procscript
;Trigger: DTLF
if ($TRIG_TRACE$)
   putmess "Entering trigger DTLF"
endif
```

## Related Topics

- [Include Proc](../../../../development/librariesglobalobjects/concepts/includescript.md)


---

# #info

Produce a compiler information message.

#info  Text

## Parameters

Text—compiler message text

## Description

The
#info directive causes the Proc compiler to generate an
information message. The message
Text appears in the message frame, and the information
message is added to the total for the module being compiled.

The following example causes
the Proc compiler to generate an information message if the compile-time
constant
`<MYCONSTANT>` has not been defined:

```procscript
#ifundefined MYCONSTANT
   #info The constant MYCONSTANT has not been defined.
#endif
```

## Related Topics

- [#error](_error.md)
- [#warning](_warning.md)


---

# #pragma

Provides a specific instruction to the compiler.

#pragma fsyntaxoverride

## Parameters

fsyntaxoverride—instructs the
compiler to allow the field syntax in a subtype entity to override that defined in its
supertype.

## Use

Allowed in all Uniface component types.

## fsyntaxoverride

When a supertype entity specifies a field syntax
of `MAN` (mandatory), it is not possible to change this in its subtype entities, or
in components that use the subtype entities. This *was* possible prior to Uniface
9.6.01. If this change in behavior causes problems, it is possible to revert to the old behavior by
using the directive #pragma fsyntaxoverride.

**Note:**  Although this directive can be set in any
trigger, it is strongly recommended that you set it in the Defines trigger of the component. This
ensures that all items in the component use this setting during compilation.

When the #pragma
fsyntaxoverride is present, and the field syntax of the subtype entity differs from the
supertype, the component can be compiled but a compiler warning is issued and is returned in
$procreturncontext (after $ude ("compile")):

```procscript
1024 warning: Field syntax for field is overwritten on component level because fsyntaxoverride pragma is set
```

| Version | Change |
| --- | --- |
| 9.6.05 X502 | Introduced |

---

# #undefine

Undefine a constant

#undefine  Constant

## Parameters

Constant—constant name

## Description

The
#undefine directive undefines the specified constant,
making it unavailable during compilation for the current scope of the
trigger.

You can use
#undefine to undefine a constant inherited from another
object, for example, the application model or a component template.

The following example undefines
the constant
`MYCONSTANT`, resulting in a warning during compilation if
`MYCONSTANT` exists:

```procscript
#undefine MYCONSTANT
#ifdefined MYCONSTANT
   #warning Constant MYCONSTANT exists
#endif
```

After compilation, no message will be generated.

## Related Topics

- [#define](_define.md)
- [#ifdefined](_ifdefined.md)
- [#ifundefined](_ifundefined.md)


---

# #warning

Produce a compiler warning message.

#warning  Text

## Parameters

Text—message text

## Description

The
#warning directive causes the Proc compiler to generate a
warning message. The message
Text appears in the message frame, and the warning message
is added to the total for the module being compiled.

The following example
causes the Proc compiler to generate a warning message if the compile-time
constant <MYCONSTANT> has not been defined:

```procscript
#ifundefined MYCONSTANT
   #warning The constant MYCONSTANT has not been defined.
#endif
```

## Related Topics

- [#error](_error.md)
- [#info](_info.md)


---

# <$cmpStateManagedBy>

State Managed By property of server page and
session service components

<$cmpStatemanagedBy>

## Values

Values can be:

* 0
  = none
* 1 = by cookie
* 2 =
  by state manager component

## Scope

Server pages and session services

Use only in the Get State and Set State triggers.

## Description

The
<$cmpStatemanagedBy> compile-time constant
corresponds to the
State Managed By property of server pages and session
services.

---

# <$componentName>

Component name

<$componentName>

## Values

Component name

## Scope

Component, entity and field triggers

## Description

The <$componentName> compile-time constant corresponds to the
component name.

---

# <$componentType>

Uniface component type

<$componentType>

## Values

| Value | Meaning |
| --- | --- |
| `A` | Startup shell |
| `F` | Form component |
| `R` | Report component |
| `D` | Dynamic server page component |
| `P` | Static server page component |
| `S` | Service component |
| `E` | Entity service component |
| `N` | Session service component |

## Scope

Component, entity, and field triggers

## Description

The
<$componentType> compile-time constant has the same
value as Proc function
 $componenttype.

---

# <$entName>

Entity name

<$entName>

## Values

Entity name

## Use

Entity-level and field-level triggers

## Description

The
<$entName> compile-time constant corresponds to the
entity name.

---

# <$fieldName>

Field name

<$fieldName>

## Values

Field name

## Use

Field-level triggers

## Description

The
<$fieldName> corresponds to the field
name.

---

# <$libraryName>

Library in which a global object resides

<$libraryName>

## Values

Library name

## Scope

Include Proc, global Proc, and menu triggers

## Description

The
<$libraryName> compile-time constant represents the
name of the library in which a global object resides.

---

# <$modelName>

Entity’s application model

<$modelName>

## Values

Name of the entity’s application model

## Scope

Entity and field triggers

## Description

The
<$modelName> compile-time constant corresponds to
the entity’s application model.

---

# <$tableName>

Entity's supertype.

<$tableName>

## Values

Name of the entity's
supertype

## Scope

Entity and field triggers.

## Description

The
<$tableName> compile-time constant corresponds to
the entity's supertype. (This corresponds to the name of the table where these
entities are stored in the Uniface Repository.)

---

# <$triggerAbbr>

Trigger abbreviation

<$triggerAbbr>

## Values

Trigger Name Abbreviations

| Abbreviation | Trigger | Level |
| --- | --- | --- |
| ACPT | <Accept> | Component |
| AIO | <Add/Insert Occurrence> | Entity |
| APPL | Application Execute | Application |
| ASYN | Asynchronous Interrupt | Application |
| ASYS | Asynchronous Interrupt | Component |
| CLR | <Clear> | Component |
| DECR | Decrypt | Field |
| DEFE | Defines | Entity |
| DEFF | Defines | Field |
| DEFN | Defines | Component |
| DELE | Delete | Entity |
| DFMT | Deformat | Field |
| DLUP | Delete Up | Entity |
| DTLE | <Detail> | Entity |
| DTLF | <Detail> | Field |
| ECOP | Collection Operations | Entity |
| ENCR | Encrypt | Field |
| EOOP | Occurrence Operations | Entity |
| ERAS | <Erase> | Component |
| ERRE | On Error | Entity |
| ERRF | On Error | Field |
| EXTG | Extended Triggers | Entity |
| EXEC | Execute | Component |
| FGF | Field Gets Focus | Field |
| FMT | Format | Field |
| FRGF | Form Gets Focus | Component |
| FRLF | Form Loses Focus | Component |
| FXTG | Extended Triggers | Field |
| GTST | Get State | Component |
| HLPE | <Help> | Entity |
| HLPF | <Help> | Field |
| LFLD | Leave Field | Field |
| LMK | Leave Modified Key | Entity |
| LMO | Leave Modified Occurrence | Entity |
| LOCK | Lock | Entity |
| LPME | Local Proc Modules | Entity |
| LPMF | Local Proc Modules | Field |
| LPMX | Local Proc Modules | Component |
| LPO | Leave Printed Occurrence | Entity |
| MNUA | <Menu> | Application |
| MNUE | <Menu> | Entity |
| MNUF | <Menu> | Field |
| MNUS | <Menu> | Component |
| NFLD | <Next Field> | Field |
| OGF 1 | Frame Gets Focus | Header, trailer, and break frame |
| OGF | Occurrence Gets Focus | Entity |
| OPER | Operations | Component |
| OPTN | Option | Menu item |
| PDIS | Predisplay | Menu item |
| PREP | Pre Request | Application |
| PRLO | Pre Load Occurrence | Entity |
| PRSO | Pre Save Occurrence | Entity |
| PFLD | <Previous Field> | Field |
| PRNT | <Print> | Component |
| PSLO | Post Load Occurrence | Entity |
| PSSO | Post Save Occurrence | Entity |
| PSTP | Post Request | Application |
| PULA | <Pulldown> | Application |
| PULS | <Pulldown> | Component |
| QUIT | <Quit> | Component |
| READ | Read | Entity |
| RETR | <Retrieve> | Component |
| RETS | <Retrieve Sequential> | Component |
| RMO | <Remove Occurrence> | Entity |
| SMOD | Start Modification | Field |
| STOR | <Store> | Component |
| STST | Set State | Component |
| SWIT | Switch Keyboard | Application |
| UKYA | <User Key> | Application |
| UKYS | <User Key> | Component |
| VALC | Value Changed | Field |
| VLDF | Validate Field | Field |
| VLDK | Validate Key | Entity |
| VLDO | Validate Occurrence | Entity |
| WRIT | Write | Entity |
| WRUP | Write Up | Entity |
|  |  |  |
| --- | --- | --- |
| 1 This abbreviation is not shown in the Uniface Debugger, but is shown in Proc code listings. | | |

## Scope

All triggers

## Description

The
<$triggerAbbr> compile-time constant corresponds to
the trigger abbreviation.

---

# Proc: Precompiler Directives and Compile-Time Constants

Precompiler directives and compile-time constants enable you to produce generalized,
reusable Proc code. They allow you to determine the exact composition of the Proc code that will be
compiled.

## Precompiler Directives

[#define](directives/_define.md)

[#entry](directives/_entry.md)

[#error](directives/_error.md)

[#for](directives/_for.md)

[#if](directives/_if.md)

[#ifdefined](directives/_ifdefined.md)

[#ifundefined](directives/_ifundefined.md)

[#include](directives/_include.md)

[#info](directives/_info.md)

[#pragma](directives/_pragma.md)

[#undefine](directives/_undefine.md)

[#warning](directives/_warning.md)

## Compile-Time Constants

**Note:**  These compile-time constants should only be used
in components, not in global Proc, because at compile time, global Proc does not have access to the
information required to correctly resolve the compile-time constants.

[<$cmpStateManagedBy>](constants/__cmpstatemanagedby_.md)

[<$componentName>](constants/__componentname_.md)

[<$componentType>](constants/__componenttype_.md)

[<$entName>](constants/__entname_.md)

[<$fieldName>](constants/__fieldname_.md)

[<$libraryName>](constants/__libraryname_.md)

[<$modelName>](constants/__modelname_.md)

[<$tableName>](constants/__tablename_.md)

[<$triggerAbbr>](constants/__triggerabbr_.md)

## Related Topics

- [Precompiler Directives](../../proclanguage/precompiler/precompilerdirectives.md)
- [Compile-Time Constants](../../proclanguage/precompiler/compiletimeconstants.md)

