# RuboCop Scripts

This directory is reserved for utility scripts that might be added in future versions.

Currently, RuboCop is typically invoked directly via command line, so no additional scripts are needed.

For custom cop development or automation needs, scripts can be added here.
