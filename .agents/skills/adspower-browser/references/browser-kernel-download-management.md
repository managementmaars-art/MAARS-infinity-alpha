# Browser kernel download

**download-kernel** — Download or update a specific browser kernel version.

- **kernel_type** (required): `'Chrome'` | `'Firefox'`
- **kernel_version** (required): Kernel version string, e.g. `"141"`

