# ===========================================
# RESEND INBOUND EMAILS - Environment Variables
# ===========================================
# Add these to your .env.local file

# Resend API Key (from resend.com dashboard)
RESEND_API_KEY="re_your_api_key"

# Resend Webhook Secret (from Resend Dashboard → Webhooks)
# Used to verify webhook signatures
RESEND_WEBHOOK_SECRET="whsec_your_webhook_secret"

# Default sender email (for system emails like password reset)
EMAIL_FROM="noreply@yourdomain.com"

# Your inbound domain for receiving emails
# All emails sent to *@this-domain will be forwarded to your webhook
# Set this up in Resend Dashboard → Domains (as inbound domain)
DEFAULT_EMAIL_DOMAIN="inbox.yourdomain.com"

# Application URL (for notification email links)
NEXT_PUBLIC_APP_URL="https://yourdomain.com"

# ===========================================
# OPTIONAL: Redis for SSE real-time updates
# ===========================================
# Only needed if using SSE instead of polling
# Get from Railway Redis or any Redis provider

# REDIS_URL="redis://default:password@host:port"
