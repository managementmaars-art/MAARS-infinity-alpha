[project]
name = "moltbot-feishu-connection"
version = "1.0.0"
description = "Connect Feishu/Lark bot to Moltbot Gateway via WebSocket"
requires-python = ">=3.11"
dependencies = [
  "lark-oapi>=1.3.0",
  "websockets>=12.0",
]

[tool.uv]
package = false
